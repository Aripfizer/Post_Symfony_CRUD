<div class="w-96 mx-auto shadow-md rounded bg-gray-50 p-4">

	{{ form_start(form) }}
	{{ form_widget(form) }}
	<button class="px-4 py-2 bg-blue-500 shadow text-white font-medium hover:bg-blue-700 transition duration-500">
		{{ button_label|default('Save') }}
	</button>
	{{ form_end(form) }}

</div>
